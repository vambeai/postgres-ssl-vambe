[build]
builder = "dockerfile"
dockerfile = "Dockerfile.16"

[phases.setup]
aptPkgs = ["openssl", "wget", "postgresql"]

[phases.build]
cmds = ["chmod +x init-ssl.sh", "chmod +x wrapper.sh", "./init-ssl.sh"]

[start]
cmd = "docker-entrypoint.sh postgres"

[variables]
PGDATA = "/var/lib/postgresql/data"
PGUSER = "postgres"
PGPASSWORD = "MTHccZteMckHfwTpDznkBpSinpnxcLIo"
PGDATABASE = "railway"
PGPORT = "5432"
SSL_CERT_DAYS = "820"
