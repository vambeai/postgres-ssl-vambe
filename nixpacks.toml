[phases.setup]
aptPkgs = ["openssl", "wget", "postgresql"]

[phases.build]
builder = "dockerfile"
dockerfile = "Dockerfile.16"

[start]
cmd = "docker run -v $(pwd)/wrapper.sh:/usr/local/bin/wrapper.sh -e PGDATA=/var/lib/postgresql/data -e PGUSER=postgres -e PGPASSWORD=MTHccZteMckHfwTpDznkBpSinpnxcLIo -e PGDATABASE=railway -e PGPORT=5432 -e SSL_CERT_DAYS=820 my_postgres_image:latest"

[variables]
PGDATA = "/var/lib/postgresql/data"
PGUSER = "postgres"
PGPASSWORD = "MTHccZteMckHfwTpDznkBpSinpnxcLIo"
PGDATABASE = "railway"
PGPORT = "5432"
SSL_CERT_DAYS = "820"
