[build]
builder = "dockerfile"
dockerfile = "Dockerfile.16"

[phases.setup]
aptPkgs = ["openssl", "wget", "postgresql"]

[phases.build]
# Ensure the init-ssl.sh script is executable and run it during the build phase
cmds = ["chmod +x /docker-entrypoint-initdb.d/init-ssl.sh", "/docker-entrypoint-initdb.d/init-ssl.sh"]

[start]
# Ensure the wrapper script has the correct path and is executable
cmd = "/usr/local/bin/wrapper.sh"

[variables]
# Set up PostgreSQL-related environment variables for your app
PGDATA = "/var/lib/postgresql/data"
PGUSER = "postgres"
PGPASSWORD = "MTHccZteMckHfwTpDznkBpSinpnxcLIo"
PGDATABASE = "railway"
PGPORT = "5432"
SSL_CERT_DAYS = "820"
